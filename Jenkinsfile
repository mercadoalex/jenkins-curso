pipeline {
    agent {  any //docker { image 'node:18.16.0-alpine' } }
    stages {
        stage('build'){
            steps{ 
                sh 'echo $PATH'
                //sh 'node --version'
            } 
        }
    }    
}
